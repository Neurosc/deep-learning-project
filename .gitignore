# This folder also holds the dataset — ignore everything by default
/*
# Re-include only code and docs
!/.gitignore
!/*.md
!/*.py
!/*.ipynb
!/*.sh

# Allow the results folder, but only the small text/figure outputs
# (CSVs and PNG figures) — never the dataset or model files.
!/results/
/results/*
!/results/*.csv
!/results/*.png
